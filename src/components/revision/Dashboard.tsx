"use client";
/* Client-only localStorage hydration is intentionally performed after mount. */
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/purity */
import { useEffect,useMemo,useState } from "react";
import Link from "next/link";
import { topicRegistry } from "@/data/topics/registry";
import { readProgress } from "@/lib/progress";
import { useHomeHref } from "@/hooks/useHomeHref";

export default function Dashboard(){const [ready,setReady]=useState(false);const homeHref=useHomeHref();useEffect(()=>setReady(true),[]);const data=useMemo(()=>{if(!ready)return null;const now=Date.now();const topics=topicRegistry.map(topic=>{const p=readProgress(topic);const valid=new Set(topic.questions.map(q=>q.id));const completed=[...p.completed].filter(id=>valid.has(id)).length;const bookmarks=[...p.bookmarks].filter(id=>valid.has(id)).length;const due=Object.entries(p.reviews).filter(([id,r])=>valid.has(id)&&new Date(r.dueAt).getTime()<=now).length;return{topic,completed,bookmarks,due,total:topic.questions.length,percent:Math.round(completed/topic.questions.length*100)}});const subjects=["biology","chemistry","physics"].map(subject=>{const rows=topics.filter(x=>(x.topic.subject??"biology")===subject);return{subject,completed:rows.reduce((n,x)=>n+x.completed,0),total:rows.reduce((n,x)=>n+x.total,0)}});const ranked=[...topics].sort((a,b)=>b.percent-a.percent);const next=[...topics].sort((a,b)=>b.due-a.due||a.percent-b.percent)[0];return{topics,subjects,total:topics.reduce((n,x)=>n+x.total,0),completed:topics.reduce((n,x)=>n+x.completed,0),bookmarks:topics.reduce((n,x)=>n+x.bookmarks,0),due:topics.reduce((n,x)=>n+x.due,0),strong:ranked[0],weak:[...ranked].reverse()[0],next};},[ready]);if(!data)return <main className="p-10">Loading your learning…</main>;return <main className="min-h-screen bg-[#f7f9fb] text-[#0f1f3d]"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-5"><Link href={homeHref} className="font-black">Sci<span className="text-[#00a551]">Mastery</span></Link><Link href="/practice" className="font-bold text-[#00a551]">Revision centre</Link></div></header><div className="mx-auto max-w-7xl px-4 py-10"><p className="text-sm font-bold uppercase tracking-widest text-[#00a551]">My Learning</p><h1 className="mt-2 text-4xl font-black">Your science dashboard</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Completed",`${data.completed} / ${data.total}`],["Attempted",String(data.completed)],["Bookmarked",String(data.bookmarks)],["Due today",String(data.due)]].map(([l,v])=><article key={l} className="rounded-2xl border bg-white p-6"><p className="text-sm font-bold text-[#5a6b82]">{l}</p><p className="mt-2 text-3xl font-black">{v}</p></article>)}</div><section className="mt-8 grid gap-6 lg:grid-cols-2"><article className="rounded-3xl border bg-white p-6"><h2 className="text-xl font-black">Progress by subject</h2>{data.subjects.map(s=><div key={s.subject} className="mt-5"><div className="flex justify-between font-bold capitalize"><span>{s.subject}</span><span>{s.completed}/{s.total}</span></div><div className="mt-2 h-3 rounded-full bg-slate-200"><div className="h-3 rounded-full bg-[#00a551]" style={{width:`${s.completed/s.total*100}%`}}/></div></div>)}</article><article className="rounded-3xl border bg-white p-6"><h2 className="text-xl font-black">Recommended next</h2><p className="mt-4 text-[#5a6b82]">{data.due?`You have ${data.due} questions due across your subjects.`:`Build your first review schedule by practising ${data.next.topic.title}.`}</p><Link href={data.due?"/practice?mode=due":data.next.topic.route} className="mt-5 inline-block rounded-xl bg-[#00a551] px-5 py-3 font-bold text-white">{data.due?"Review due questions":`Continue ${data.next.topic.title}`}</Link><div className="mt-7 grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#e9f8f0] p-4"><p className="text-xs font-bold uppercase">Strongest</p><p className="mt-1 font-black">{data.strong.topic.title}</p></div><div className="rounded-xl bg-[#fff4e5] p-4"><p className="text-xs font-bold uppercase">Needs focus</p><p className="mt-1 font-black">{data.weak.topic.title}</p></div></div></article></section></div></main>}
